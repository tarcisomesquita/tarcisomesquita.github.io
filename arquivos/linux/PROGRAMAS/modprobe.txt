MODPROBE(8)
03 March 2009

NAME
modprobe - program to add and remove modules from the Linux Kernel

SYNOPSIS
modprobe [ -v ] [ -V ] [ -C config-file ] [ -n ] [ -i ] [ -q ] [ -b ] [ -o modulename ] [ modulename ] [ module parameters ... ]

modprobe [ -r ] [ -v ] [ -n ] [ -i ] [ modulename ... ]

modprobe [ -l ] [ -t dirname ] [ wildcard ]

modprobe [ -c ]

modprobe [ --dump-modversions ]

DESCRIPTION
modprobe adds or removes a module from the Linux kernel.
There is no difference between _ and - in module names.
modprobe looks in /lib/modules/`uname -r`

module failure is sometimes accompanied by a kernel message: see dmesg(8).

modprobe expects an up-to-date modules.dep file, as generated by depmod (see depmod(8)). modprobe uses this to add or remove the dependencies automatically.

If any arguments are given after the modulename, they are passed to the kernel.

OPTIONS
-v --verbose

-c --showconfig
       Dump out the configuration file and exit.

-n --dry-run --show
       This option does everything but actually insert or delete the modules (or run the install or remove commands).  Combined with -v, it is useful for debugging problems.

-i --ignore-install --ignore-remove
       This option causes modprobe to ignore install and remove commands in the configuration file (if any), for the module on the command line (any dependent modules are still subject to commands set for them in the configuration file).  See modprobe.conf(5).

-q --quiet
       Normally modprobe will report an error if you try to remove or insert a module it can't find (and isn't an alias or install/remove command).  With this flag, modprobe will simply ignore any bogus names (the kernel uses this to opportunistically probe for modules which might exist).

-r --remove
       This option causes modprobe to remove, rather than insert a module.  If the modules it depends on are also unused, modprobe will try to remove them, too.  Unlike insertion, more than one module can be specified on the command line (it does not make sense to specify module parameters when removing modules).

-V --version

-a --all
       Insert all module names on the command line.

-D --show-depends
       List the dependencies of a module (or alias), including the module itself.
modinfo(8) can be used to extract dependencies of a module from the module itself, but knows nothing of aliases or install commands.

-b --use-blacklist
       This option causes modprobe to apply the blacklist commands in the configuration file (if any) to module names as well. It is usually used by udev(7).

-d --dirname
       Directory where modules can be found, /lib/modules/RELEASE by default.

/etc/modprobe.d contem a blacklist com os modules que não devem ser carregados.

SEE ALSO
modprobe.conf(5), lsmod(8), modinfo(8), modprobe.old(8)

