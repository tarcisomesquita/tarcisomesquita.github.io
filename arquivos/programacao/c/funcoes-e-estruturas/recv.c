
// Retorna o numero de caracteres escritos em BUFFER
// Retorna 0 se a conexao for fechada
// FLAGS = 0

int recv(int FD, char *BUFFER, int LEN, int FLAGS)

// send() and recv() are very similar to read() and write(), but have some additional operational flags.

