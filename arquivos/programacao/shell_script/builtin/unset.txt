
unset [-fv] name ...

The specified variables and functions are unset and unexported.
Apaga uma variável.

If -f or -v is specified, the corresponding function or variable is unset, respectively.

If a given name corresponds to both a variable and a function, and no options are given, only the variable is unset.

